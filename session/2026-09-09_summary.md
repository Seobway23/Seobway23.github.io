# 세션 요약 — 2026-09-09

## 한 일

### 1. 카테고리 재구성 — `study/frontend/**` → `frontend/**`

사용자 요구: 최상위를 `study / frontend / work` 형제 관계로. frontend 아래는 `javascript`.

- `git mv` 로 9개 하위 폴더 이동(히스토리 유지). `js` 와 `javascript` 를 `javascript` 로 병합.
- frontmatter `category` + 소스·설정·문서의 문자열 참조 **35개 파일 일괄 치환**.
- 결과: 최상위 `study 81 · frontend 29 · work 2`, 잔여 `study/frontend` 참조 0.
- `DOMAIN_TO_CATEGORY`(`scripts/build-concept-graph.mjs`)도 함께 갱신 →
  미작성 개념도 frontend 지도에 자리를 유지.
- 이관 스크립트는 `--apply` 없이 미리보기부터 돌려 계획을 확인한 뒤 적용.

`study` 하위(backend·network·infra·ai·electron 등 81글)는 그대로 뒀다.
사용자가 frontend 만 최상위로 올리라고 지정했다.

### 2. 개념 글 5개 작성 (js-core 3/12 → 8/12)

| 개념 | 글 | 담은 것 |
|---|---|---|
| `call-stack` | 콜 스택 | seq 7단계(쌓임→빠짐), 스택 깊이 측정 playground, 스택 트레이스 읽는 법 |
| `primitive-type` | 원시 타입 | 7가지 표, `typeof` 판별 playground, `typeof null` 함정 |
| `reference-type` | 참조 타입 | 주소 복사 seq, 얕은 복사에서 중첩 배열이 공유되는 것 실증 |
| `execution-context` | 실행 컨텍스트 | 컨텍스트 3요소, 생성/실행 2단계 split, `outer` = 정의 위치 실증 |
| `prototype` | 프로토타입 | 체인 탐색 seq 5레인, 연결 방법 3탭(Object.create/생성자/class), 체인 순회 playground |

- 출처 전부 MDN · ECMAScript 사양. 검증 통과: 개념 글 9개 · 출처 26개.
- 로드맵 LV0~LV1 이 전부 "작성됨"으로 채워졌다.

## 검증

- `npm run verify` 통과 → `npm run build` 성공 (글 112개 프리렌더).
- 5개 글 브라우저 실측: seq·playground 전부 렌더, 파서 오류 0.
  (레인 수 2·3·4·4·5, 각 글 playground 1개)
- 카테고리 뷰: 사이드바 최상위 3개(`frontend 29 / study 81 / work 2`) 확인,
  로드맵 토글에서 `9/20 작성됨` · `js-core 8/12` 확인.
- `llms.txt` 가 레벨 순 정렬 + 선행 관계 포함으로 갱신됨.

## 남은 것

- js-core 나머지 4개: `tdz` · `this` · `arrow-function` · `shallow-deep-copy`
- js-async 4개(`task-queue`·`microtask`·`promise`·`async-await`), js-functional 3개
- Phase 1 UI(선행 브레드크럼 · 자동 "다음 읽을 글" · 링크 호버 프리뷰) 미착수
- `study/` 하위의 나머지 도메인(backend·network·infra…)도 최상위로 올릴지는 미결
