# 세션 요약 — 2026-09-07

블로그를 "읽는 글"에서 "돌려 보는 글"로 바꾸는 작업. 목차 고정 버그 수정,
본문 인터랙티브 블록 4종 추가, SEO 프리렌더 도입, 작성 매뉴얼 작성.

## 한 일

### 1. 목차 sticky 수정
- 원인: `overflow-x: hidden` 이 `overflow-y` 를 `auto` 로 승격 → 스크롤하지 않는
  스크롤 컨테이너가 생겨 `position: sticky` 가 뷰포트 대신 그 박스를 기준으로 삼음.
- 걸린 곳 3군데: `src/index.css` `html`(71) · `body`(83), `src/components/layout.tsx:32`.
  전부 `overflow-x: clip` 으로 교체(가로 넘침은 그대로 막고 스크롤 컨테이너는 안 만든다).
- 검증: `scrollY 2500` 에서 sticky 요소 `top === 96px`.

### 2. 오른쪽 사이드바 정리
- 글 정보·태그·작성자 카드는 본문 헤더/하단과 중복 → 삭제. 목차만 남김.
- 작성자 소개는 본문 하단(댓글 위)으로 이동.
- 사이드바 높이 1000px → 357px, 스크롤바 2개 → 0개.

### 3. 본문 확장 블록
| 문법 | 구현 |
|---|---|
| `::: tabs` / `::: split` / `::: grid` | `scripts/generate-posts-data.js` `processSectionBlocks` (깊이 카운트로 콜아웃 중첩 지원) + `post.tsx` `initPostTabs` |
| ` ```seq ` | `src/lib/post-sequence.ts` — 줄 단위 DSL 파서 + 0..n 재생 상태 머신 + FLIP 애니메이션 + SVG 화살표 + 재생/스텝/키보드 조작 |
| ` ```playground ` | `src/lib/post-playground.ts` — iframe srcdoc + importmap(esm.sh) + @babel/standalone, console → postMessage |
| `- [ ]` 체크리스트 | `src/index.css` (GFM 이 이미 렌더, 스타일만) |

- preset 사양은 `shared/seq-presets.mjs` 로 분리 — 브라우저와 프리렌더가 공유.
- 스타일 전부 `src/index.css` 맨 아래 "본문 확장 블록" 구역.

### 4. SEO 프리렌더
- `scripts/prerender.mjs` 신규. `npm run build` 마지막 단계로 편입.
- 글 104개 → `dist/post/<slug>.html` + `dist/post/<slug>/index.html`
  (본문 HTML 삽입, 글별 title/description/canonical/OG/Twitter, JSON-LD
  BlogPosting + BreadcrumbList), `sitemap.xml`(106 URL), `robots.txt`(AI 크롤러 명시 허용).
- ` ```seq ` 스펙 원문은 `step` 설명 `<ol>` 로, ` ```playground ` 옵션 줄은 제거해 노출.
- 커스텀 도메인 시 `SITE_URL=... npm run build`.

### 5. 문서
- `posts/README.md` 신규 — md 에서 쓸 수 있는 기능 전체 매뉴얼.
- `CLAUDE.md` 에 인터랙티브 블록·SEO 요약과 포인터 추가.
- 예시 글 `posts/study/frontend/js/event-loop-interactive.md` 추가.

### 6. 마감 (2회차)
- **회귀 검사**: 기존 104개 글 전수 — 미치환 플레이스홀더 0, 남은 `:::` 0.
  렌더 통계 유지(콜아웃 98 · 코드탭 8 · KaTeX 416 · mermaid 220 · 도메인카드 4).
- **모바일(390px)**: 가로 넘침 없음. 레인 2열 접힘에 맞춰 화살표 앵커를
  레인 상단 → 세로 중심으로 변경(`sameRow` 분기), 레인 `min-height` 5.5rem 로 축소.
- **라이트 테마 버그 수정**: 플레이그라운드 iframe CSS 가 다크 하드코딩이라
  라이트에서 내용이 안 보였다. `frameCss(dark)` 두 벌 + 실행 시점 테마 주입.
  테마 토글 시 srcdoc `#1f2937` ↔ `#e5e7eb` 전환 확인.
- 중복이던 `MutationObserver` 제거(하이드레이션 `useEffect` deps 가 이미 `[post, theme]`).
- `::: split` 실렌더 확인(2열 405px, 라벨 2개, 내부 코드블록 하이라이팅 정상).

### 7. 용어 툴팁 · 코드 색상 테마 (3회차)
- **툴팁**(`post-glossary-layer.tsx`): `mouseout` 즉시 닫힘 → 220ms 유예 +
  패널 `onMouseEnter` 가 예약 취소 + **클릭 시 고정**(재클릭·닫기·Esc·바깥 클릭으로 해제).
  포털 패널이라 용어의 DOM 자식이 아니어서 생긴 문제. 9단계 시나리오 전부 통과.
- **코드 색상 테마**(`src/lib/code-theme.ts` 신규): hljs 스타일시트 6쌍
  (github / atom-one / stackoverflow / tokyo-night / a11y / intellij·vs2015).
  `index.html` 의 `link.light-syntax`·`link.dark-syntax` 의 href 만 갈아 끼운다.
  `use-theme.tsx` 가 테마 전환 때 함께 적용, `localStorage["code-theme"]` 로 유지.
- **설정 UI**: `background-customizer.tsx` 다이얼로그 제목을 "화면 설정"으로 바꾸고
  코드 테마 섹션을 **맨 위**에 배치(그라데이션 9개 밑에 묻히지 않게) + 실시간 미리보기.
- **편집기 문법 강조**: `<pre class="post-pg__hl">` 를 `<textarea>` 뒤에 포개고
  글자만 투명 처리, 스크롤 동기화. hljs 언어는 playground 모듈에서 별도 등록
  (js/ts/xml). `.prose` 선택자 격을 맞춰야 정렬됨.

### 8. 편집기 정렬 · 매뉴얼 이전 (4회차)
- **커서 어긋남**(사용자 지적): 글자를 그리는 `<code>` 가 `.prose code` 의
  `font-size: 0.875em` 을 먹어 11.09px/18.30px, textarea 는 12.48px/20.59px.
  줄당 2.29px 누적 → 7줄에 한 줄. `.prose .post-pg__hl code` 에 `font: inherit`.
  검증도 교체: 20줄 넣고 `Range` 로 줄별 실제 y 측정 → **누적 오차 0.03px/20줄**.
- **가로 스크롤 미동기화**: `.prose pre code` 의 `overflow:auto` 로 code 가 자체
  스크롤 컨테이너가 되어 `pre.scrollLeft` 무효. `overflow: visible` +
  `width: max-content; min-width: 100%` 로 pre 하나만 스크롤하게 함.
- **매뉴얼 이전**: `docs/POST_AUTHORING.md` → **`posts/README.md`**.
  `CLAUDE.md`·세션·릴리스 노트의 참조 경로와 포트(5173→5174) 일괄 갱신.
- **부작용 수정**: `posts/README.md` 가 글로 파싱돼 105개가 됐다.
  `getAllMarkdownFiles()` 에 제외 규칙 추가(`README.md`·`_TEMPLATE.md`·`index.md`·`_*`).

## 검증
- `npm run generate:posts` → 104개 파싱, 플레이스홀더 잔재 0.
- 편집기: 20줄 누적 오차 0.03px, 가로/세로 스크롤 동기화 true,
  가로 250px 스크롤 상태에서 선택 영역이 한글 글자에 정확히 얹힘(스크린샷).
- 툴팁 9단계 · 코드 테마 전환(github-dark→tokyo-night-dark, 키워드색
  `rgb(255,123,114)`→`rgb(187,154,247)`) · 새로고침 후 유지 · 본문 코드 블록 동시 적용 확인.
- 편집기 오버레이 `aligned: true` / `styleMatch: true`, 타이핑 시 재하이라이팅 확인.
- 브라우저 실측(gstack browse): 시퀀스 9스텝 상태 전이·화살표, 탭 3개,
  플레이그라운드 `js`(출력 1→2→3→4 순서 정확)·`react`(esm.sh import 마운트) 확인.
- 데스크톱 1440 · 모바일 390 × 라이트/다크 4조합 스크린샷 확인.
- `npm run build` 성공 + 프리렌더 104개 + sitemap 106 URL.
- `tsc --noEmit`: 신규/수정 파일 오류 없음(저장소 기존 오류는 그대로).

### 9. 개념 그래프 Phase 0 (5회차)
학습자료 10장(h2 120 · h3 324 · 용어 251 · 툴팁 276)을 마이크로 개념으로 쪼개
바텀업으로 잇는 구조. 설계안 `docs/CONCEPT_GRAPH_PLAN.md`.

- **핵심 설계**: 사람은 `prereq` 만 적고 나머지(레벨·순서·다음 글·잠금·진도)는 계산.
  `prereq` 는 **개념의 성질**이라 `data/concepts.yml` 한 곳에만 둔다.
  글 frontmatter 는 `concept:` 한 줄로 붙는다(양쪽에 적으면 어긋난다).
- `data/concepts.yml` — 개념 20 · 트랙 3(js-core 12 / js-async 5 / js-functional 3)
- `scripts/build-concept-graph.mjs` → `public/concept-graph.json`
  위상정렬로 level·levels·next·chain 계산. 순환·없는 선행·중복 주장·
  **트랙 순서가 선행을 어기는 경우**를 빌드 실패로 잡는다.
- `scripts/verify-sources.mjs` — 1차 공식 문서 화이트리스트.
  음성 테스트로 `medium.com` 출처가 실제로 빌드를 막는 것 확인.
  YAML 이 `checked: 2026-09-07` 을 Date 로 파싱하는 것 정규화.
- `posts/_TEMPLATE_concept.md` + 샘플 3글(`scope` → `hoisting` → `closure`),
  기존 `event-loop-interactive` 를 `event-loop` 개념에 연결.
- `package.json`: `graph` · `verify:sources` · `verify` 추가, `build` 와
  `generate:posts` 에 편입.

계산 결과: 깊이 6레벨, `closure.chain = scope → hoisting → closure`.

- **UI 설계 결론**(문서 §3~§4): 4층 구조 — 글 안(브레드크럼·툴팁·호버 프리뷰·다음 글) /
  사이드바 로컬 그래프 / `/roadmap` 레벨 레인 뷰 / `/track/:id`.
  **전체 force 그래프를 첫 화면에 두지 않는다**(스파게티가 되어 아무도 안 쓴다).

### 10. 로드맵 UI · 카테고리 토글 · AI 검색 (6회차)
- **`/roadmap` 페이지**: 레벨 레인 + 곡선 연결선. 배치는 CSS 가 하고 연결선은
  그려진 뒤 `getBoundingClientRect()` 로 재서 SVG 베지어로 얹는다(좌표 수계산 없음).
  노드 상태 4종(읽음·미독·잠김·미작성), 도메인 색 상단 테두리, hover 시 그 노드의
  선행·후행만 남김, 진도 localStorage(`concept-read`).
- **재사용 분리**: `src/components/concept-map.tsx` 로 지도를 빼고
  `/roadmap`(전체)과 카테고리 뷰가 같은 컴포넌트를 쓴다.
- **카테고리 안 `리스트 | 로드맵` 토글**: `home.tsx`. 개념 지도가 있는 카테고리에서만
  뜬다. `frontend` 선택 → 20개 노드·6레벨 지도 확인.
- 빌더에 `node.category` 추가 — 글이 있으면 그 글의 category, 없으면
  `DOMAIN_TO_CATEGORY` 기본값(미작성 개념도 지도에 자리를 잡게).
- **`llms.txt` 생성**(`prerender.mjs`): 글 목록 + 한 줄 요약 + **선행 관계**,
  개념 글은 레벨 순. `robots.txt` 에 위치 명시. 221줄/37KB.
- **AI 검색 전략을 `posts/README.md` §10-A 에 문서화**: 검색이 페이지가 아니라
  문단(chunk) 단위로 인용된다는 전제에서 7개 규칙(질문형 소제목 / 첫 문단에 답 /
  대명사 금지 / `<용어>는 <정의>다` / 구체적 값·날짜 / 표·목록 / excerpt = 한 문장 답).
- `/study/roadmap/` 정적 섬은 그대로 유지(200 확인).

### 결정 (사용자)
- `study/` 카테고리 접두사 제거는 **권장하되 지금은 보류**. 105/107이 `study` 라
  최상위 트리가 무의미하지만, 개념↔카테고리 매핑을 다시 잡아야 하므로 이관 때 같이.

## 환경 메모
- dev 서버는 **5174 고정**(사용자 지시). `npm run dev -- --port 5174 --strictPort`.
- `.gitignore` 에 `.gstack/` 추가됨(브라우저 도구 산출물).

## 남은 것 (사용자 확인 대기)
- 커밋/푸시 안 함.
- Google Search Console · Bing Webmaster 에 sitemap 제출은 사람이 해야 함.
- 예시 글(`posts/study/frontend/js/event-loop-interactive.md`)을 유지할지 지울지 미정.
