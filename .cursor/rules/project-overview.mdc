---
description: ModernDevBlog 프로젝트 개요 및 아키텍처 - 항상 적용되는 핵심 컨텍스트
alwaysApply: true
---

# ModernDevBlog 프로젝트 개요

## 아키텍처 패턴
- **정적 SPA 블로그**: 빌드 타임에 JSON 생성, 런타임 API 없음
- **GitHub Pages 배포**: GitHub Actions로 push 시 자동 빌드/배포
- **데이터 흐름**: `posts/*.md` → 빌드 스크립트 → `public/*.json` → React 런타임

## 기술 스택
- **React 18** + **TypeScript 5.6** + **Vite 5**
- **라우팅**: wouter (`/`, `/post/:slug`, `/about`)
- **상태관리**: TanStack React Query v5 (서버 상태)
- **UI**: shadcn/ui + Radix UI + Tailwind CSS 3.4
- **애니메이션**: Framer Motion 11
- **아이콘**: lucide-react + react-icons

## 빌드 파이프라인 순서
```
npm run generate:posts   # posts/*.md → public/posts.json
npm run fetch:views      # GA Data API → public/views.json
npm run fetch:comments   # GitHub API → public/comments.json
npm run build            # Vite 프로덕션 빌드
```

## 경로 alias
- `@/` → `src/`
- `@shared/` → `shared/`

## 환경 변수
| 변수 | 용도 |
|------|------|
| `VITE_GA_MEASUREMENT_ID` | GA4 클라이언트 측정 ID |
| `GA_PROPERTY_ID` | GA4 서버 Property ID |
| `GA_SERVICE_ACCOUNT_KEY` | GA 서비스 계정 JSON |
| `COMMENTS_GH_PAT` | GitHub PAT (댓글 수 수집, 로컬 `.env`·Actions 시크릿 동일 이름) |
| `UTTERANCES_REPO` | 댓글(Issues) 저장소 `owner/repo` (로컬 `.env`·Actions 시크릿 동일 이름) |

GitHub Actions 시크릿 이름은 `GITHUB_` 접두사 불가이므로 위 이름 사용. Pages 배포는 `github.token` 사용.

## 중요 규칙
- `server/` 폴더는 존재하지만 실제로는 정적 SPA로 동작
- 조회수/댓글 수는 빌드 타임에 수집되어 정적 파일에 포함됨
- 마크다운 폴더 구조(`posts/frontend/css/`) = 카테고리 계층 구조

## 블로그 원고 작성·편집 시 (항상 기억)
작업이 **`posts/**/*.md` 초안·수정·리뷰**를 포함하면, 저장소 **루트의 `CLAUDE.md`** 를 열어 블로그 섹션을 준수한다. 상세 워크플로·수식·시각화 프리셋은 **`.cursor/skills/blog-post-writing/SKILL.md`** 를 함께 본다. (`posts/` 편집 시 `.cursor/rules/blog-content-management.mdc` 에서도 동일 안내.) Cursor·Antigravity 등 에이전트용 요약은 **`AGENTS.md`** 를 참고한다.

**역학 시리즈** (`posts/study/mechanics/`): **`docs/mechanics-post-principles.md`** 를 반드시 참고한다.
