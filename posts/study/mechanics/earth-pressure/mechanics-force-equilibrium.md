---
title: "옹벽 힘의 평형 — Ph, 저면 마찰, 미끄럼·전도와 안전율 (토압 시리즈 실전 1)"
slug: mechanics-force-equilibrium
category: study/mechanics/earth-pressure
tags: [역학, 토질역학, 정역학, 자유물체도, 평형, 옹벽, 안전율]
author: Seobway
readTime: 18
featured: false
createdAt: 2026-04-05
excerpt: >
  Ph를 ‘이미 구해 둔 수평 합력’으로 두고, 저면 반력·수평 마찰·모멘트로
  미끄럼·전도 안전율까지 유도한다. 그림은 설명하는 바로 그 옆에 둔다.
---

## 시리즈에서의 위치

이 글은 [토압 개요 →](/post/earth-pressure-overview)에서 말한 **$P_h$** 를 **이미 알고 있다**고 가정하고, **옹벽이 안 미끄러지고 안 넘어지는지**를 **힘과 모멘트**로 검사하는 단계다. **수평·수직 힘**, **모멘트**가 낯설면 먼저 [역학 기초 1 →](/post/mechanics-basics-forces-intro), [역학 기초 2 →](/post/mechanics-basics-moment-intro)를 본 뒤 돌아오면 된다.

---

## $P_h$를 다시 한 번 (이 글의 출발점)

**$P_h$** 는 벽 뒤 흙이 벽면에 주는 **수평 토압들을 전부 합친 합력**이다. 크기는 뒤편에서 랭킨·쿨롱 등으로 구하지만, **평형식**만 쓸 때는 “**흙이 벽을 옆으로 밀어 대는 화살표 하나**”로 생각하면 된다.

**합력의 작용점**은 토압이 **아래로 갈수록 커지는** 단순 모형에서는 벽면 **하단(기초와 만나는 근처)** 쪽에 가깝고, 대표적으로 **벽면 발에서 위로 $H/3$** 에 놓는 경우가 많다($H$는 흙의 대표 높이). 아래 그림의 **노란 점**이 그 위치다.

```diagramatics
{"preset":"pressure_triangle_resultant"}
```

---

## 미끄럼과 전도를 한 줄로

- **미끄럼(sliding)** — 옹벽 **덩어리 전체**가 기초 **저면**을 따라 **옆으로 밀려 나가려는지** 본다.
- **전도(overturning)** — 벽이 **앞쪽 발(토e)** 을 축으로 **앞으로 넘어가려는지** 본다. 이때 **모멘트**가 중심이 된다. 개념은 [모멘트 글 →](/post/mechanics-basics-moment-intro)와 같다.

※ **‘활동’**은 토질에서 종종 **주동(active)** 을 가리키는 말이 섞여 있다. **바닥을 따라 밀리는 것**은 이 글에서 **미끄럼**으로 통일한다.

---

## 자유물체도(FBD)란 무엇인가

**자유물체도**는 관심 있는 물체(여기서는 **옹벽 강체 하나**)를 상상 속에서 주변과 **잘라 낸 뒤**, 잘린 면에 작용하는 **모든 외력을 화살표로 그린 그림**이다. “벽 안의 응력”이 아니라 **경계에 닿는 힘**만 본다.<a href="https://en.wikipedia.org/wiki/Free_body_diagram" target="_blank"><sup>[1]</sup></a>

---

## 저면이란, 수평 마찰 $T$란

- **저면**은 **기초 바닥**과 **지반**이 맞닿는 **접촉면**이다. 벽 자중 $W$ 같은 수직 하중이 이 면을 누르면, 지반은 **수직 반력 $R_n$** 으로 맞받친다.
- **수평 마찰 $T$** 는 그 저면에서 **옆으로 미끄러지지 않게** 잡아 주는 **전단(마찰) 저항**이다. 미끄럼 검토에서 “**$P_h$를 저면이 옆으로 버텨 주는가?**”가 핵심이다.

아래는 **단순화한** 옹벽 FBD다. 색과 라벨이 위 기호와 대응한다.

```diagramatics
{"preset":"retaining_wall_fbd"}
```

---

## 수평 힘만 먼저 — 직관 그림

**수평 방향**으로는 “흙이 밀고” vs “저면이 막는다”가 맞선다. [기초 1 →](/post/mechanics-basics-forces-intro)에서 말한 $\sum F_x = 0$ 직관을 같은 스타일로 다시 그리면 아래와 같다.

```diagramatics
{"preset":"balance_horizontal"}
```

---

## 기호 표

| 기호 | 뜻 |
|------|-----|
| $W$ | 벽 **자중** (아래 방향) |
| $P_h$ | 흙이 벽에 주는 **수평 토압 합력** |
| $P_v$ | 벽면 마찰 등으로 생기는 **수직 성분**(단순 모델에서는 0으로 두기도 함) |
| $R_n$ | 기초 **저면**에서 지반이 주는 **수직 반력** |
| $T$ | 저면 **수평 마찰(전단) 저항** — 미끄럼을 막는 힘 |
| $B$ | 기초 너비 |
| $H$ | 벽 뒤 흙의 대표 높이(합력 작용 높이 스케일) |

---

## 가정 (단순화 모델)

- 벽은 **강체** — 변형은 무시하고 힘만 본다.
- **평면 변형** — 깊이 방향으로 단면이 같다고 본다.
- 한동안 **$P_v = 0$** 이면 수직 평형은 $R_n = W$ 로 단순화한다.
- 저면 마찰은 **쿨롱 마찰**로 $T \le R_n \tan\delta_b + c_b B$. 한계에서는 $T_{\max} = R_n \tan\delta_b + c_b B$. $\delta_b$는 기초–지반 **마찰각**, $c_b$는 저면 **점착**(없으면 0).

---

## 수평·수직 힘의 평형

정지해 있으면

$$
\sum F_x = 0, \qquad \sum F_y = 0.
$$

**수평:** 다른 수평력이 없다고 하면 $P_h - T = 0$ → **미끄럼이 일어나지 않으려면** $T = P_h$ 가 되어야 한다. 즉 저면이 **최소한 $P_h$만큼** 옆으로 받쳐 줘야 한다.

**수직:** $P_v=0$ 이면 $R_n = W$.

---

## 모멘트 평형과 합력의 작용점

한 점 $O$에 대해 **넘어지게 하는 모멘트**와 **막는 모멘트**가 맞서야 한다. 전도 검토에서는 $O$를 **토e(앞쪽 모서리)** 에 두는 경우가 많다 — “앞으로 넘어가나?”를 보기 좋기 때문이다. 모멘트 정의 자체는 [기초 2 →](/post/mechanics-basics-moment-intro)와 같다.

**$P_h$의 작용점**이 기초면에서 **위로 $\bar{z}$** 에 있다고 하면, $O$에 대한 전도 모멘트는 대략 $M_{\text{ov}} = P_h \bar{z}$ 이다. 삼각형 분포에서 $\bar{z} = H/3$ 인 경우는 아래 유도를 본다.

---

## 유도: 삼각형 분포 토압의 합력과 작용점

$z$를 **지표에서 아래로 잰 깊이**, $0 \le z \le H$ 라 두자. 벽면 수평 압력이 **깊이에 선형**으로 커진다고 하면

$$
p(z) = p_0 \frac{z}{H}
$$

($z=0$ 에서 0, $z=H$ 에서 $p_0$).

**(1) 합력 크기** — 분포력의 합은 **압력도의 면적**(단위 깊이 폭당).

$$
P_h = \int_0^H p(z)\,dz = \frac{1}{2} p_0 H.
$$

**(2) 토e 축에 대한 모멘트** — 깊이 $z$의 작은 띠가 만드는 힘은 $p(z)\,dz$ 이고, 팔의 길이는 $(H-z)$ 이다.

$$
M_O = \int_0^H p(z)(H-z)\,dz = \frac{1}{6}p_0 H^2.
$$

합력 $P_h$가 기초면에서 **위로 $\bar{z}$** 에서 작용하면 $M_O = P_h\,\bar{z}$ 이므로

$$
\bar{z} = \frac{M_O}{P_h} = \frac{H}{3}.
$$

---

## 깊이–압력 그래프 (선형 $p(z)$ 복습)

위 유도에서 “**면적 = $P_h$**” 를 떠올리기 쉽게, 선형 $p(z)$ 두 개의 기울기를 비교하는 **예시 그래프**를 한 번 더 둔다(아직 특정 $K$ 값 대입은 하지 않는다).

```jsxgraph
{"preset":"ka_kp_linear"}
```

---

## 안전율이란 무엇인가

**안전율(factor of safety)** 은 “**버틸 수 있는 최대**”를 “**실제로 요구되는 것**”으로 나눈 **여유 배수**로 생각하면 된다.

- **1.0 근처** — 한계에 가깝다(설계에서 보통 피한다).
- **1.5~1.75** 등 — 규범·지침에서 자주 요구하는 **하한 예시**에 가깝다(국가·사양마다 다름).

아래 두 식은 **미끄럼**과 **전도**에 대해 각각 그 비율을 정의한 것이다.

---

## 유도: 미끄럼 안전율

저면이 줄 수 있는 **최대** 수평 저항(마찰 한계)은

$$
T_{\max} = R_n \tan\delta_b + c_b B.
$$

평형에서 $T = P_h$ 이므로, **파괴 직전**에는 $P_h = T_{\max}$. **미끄럼 안전율**은

$$
FS_{\text{sl}} = \frac{T_{\max}}{P_h} = \frac{R_n \tan\delta_b + c_b B}{P_h}.
$$

$R_n \approx W$, $P_v=0$ 이면 $FS_{\text{sl}} = \dfrac{W\tan\delta_b + c_b B}{P_h}$.<a href="https://en.wikipedia.org/wiki/Retaining_wall" target="_blank"><sup>[2]</sup></a>

---

## 유도: 전도 안전율(한 가지 정의)

$O$를 **토e**에 두고,

- **전도 모멘트** $M_{\text{ov}} \approx P_h \bar{z}$ (삼각형이면 $\bar{z} \approx H/3$).
- **복원 모멘트** $M_{\text{res}}$ — $W$와 지반 반력 분포가 만드는 **넘어짐을 막는** 모멘트(벽이 뒤쪽으로 무게중심이 있을수록 커지기 쉽다).

$$
FS_{\text{ov}} = \frac{M_{\text{res}}}{M_{\text{ov}}}.
$$

직사각형 단면 벽만 예로 들면 $M_{\text{res}} \approx W \cdot (b/2)$ 같은 꼴이 나온다. 실무에서는 **지반 반력 분포**를 더 정밀히 넣어 $M_{\text{res}}$를 잡는다.

---

## 다음에 올 이론(예고)

$P_h$ 자체를 구하려면 **응력**, **유효응력**, **모어의 원**, **모어–쿨롱**, $K_0$, **랭킨**, **쿨롱** 순으로 돌아올 예정이다. 지도는 [토압 개요 →](/post/earth-pressure-overview)와 **`docs/earth-pressure-series-roadmap.md`** 를 보면 된다.

---

## 한 줄 정리

> $P_h$는 흙이 벽을 옆으로 밀어 대는 **합력**이고, **미끄럼**은 저면 **수평 마찰**이 그것을 버티는지, **전도**는 **작용점 높이**까지 포함한 **모멘트**로 본다. **안전율**은 그 버팀이 얼마나 **여유 있게** 큰지를 숫자로 나타낸다.

---

## 참고

<a href="https://en.wikipedia.org/wiki/Free_body_diagram" target="_blank">[1] Free body diagram — Wikipedia</a>

<a href="https://en.wikipedia.org/wiki/Retaining_wall" target="_blank">[2] Retaining wall — Wikipedia</a>

<a href="https://en.wikipedia.org/wiki/Statics" target="_blank">[3] Statics — Wikipedia</a>

---

## 관련 글

- [토압이 뭔가요? — 시리즈 개요 →](/post/earth-pressure-overview)
- [힘과 평형의 말풀이 (역학 기초 1) →](/post/mechanics-basics-forces-intro)
- [모멘트와 작용점 (역학 기초 2) →](/post/mechanics-basics-moment-intro)
